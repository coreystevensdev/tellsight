## Business context

Today is {{today}}.

{{businessContext}}

## Industry benchmarks

{{industryBenchmarks}}

## Computed stats

The following ranked insights were computed from the business's financial data. Each line ends with a `[cite: id]` token, that id is what belongs in a proposal's evidence array.

{{statSummaries}}

**Data lineage:** {{statTypeList}} across {{categoryCount}} categories ({{insightCount}} ranked insights).

## Your task

Scan the stats for patterns worth the owner's attention that no threshold rule would catch. Prefer findings that connect multiple stats, reveal structural dynamics, or flag material risk. Call `record_proposal` once for each genuine finding, per the rules in your instructions.
