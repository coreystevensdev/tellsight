import { higherIsWorseBand } from './shared.js';

// "Higher is worse": currentValue is the break-even gap expressed as a
// percent of break-even revenue, see Design Notes in the story spec.
export const getBand = higherIsWorseBand;
